# Optional local secrets used by docker-compose.yml.
# Copy this file to .env and fill only the values you need.
CLOUDINARY_URL=

# Used by the distance calculator geocoder and Mapbox UI integrations.
MAPBOX_API_KEY=

# Optional: set these if your host user is not 1000:1000.
UID=1000
GID=1000
